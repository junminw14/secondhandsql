# 代码备份 - 2026-04-27

## 当前项目状态
- 使用 SQLite 本地数据库
- 部署到 Vercel 后查询正常，但写入操作（插入、删除、修改、重置）失败
- 准备迁移到 Supabase 数据库

## 关键文件备份

### app.py 关键函数
- `get_db()`: 使用 SQLite 连接
- `init_database()`: 初始化 SQLite 数据库
- `purchase_item()`: 事务购买逻辑
- 所有路由函数

### 数据库相关文件
- `schema.sql`: SQLite 表结构
- `init_data.sql`: 初始数据

## 迁移计划
1. 替换 `get_db()` 函数，使用 Supabase 连接
2. 调整数据库操作语句
3. 配置环境变量
4. 测试所有功能
